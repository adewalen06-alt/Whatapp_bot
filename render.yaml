services:
  - type: web
    name: topai-whatsapp-bot
    runtime: docker
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
    autoDeploy: true

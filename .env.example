# Server Configuration
PORT=7860

# Optional: Pre-load a session string (from a previous pair)
# Paste the full KnightBot! session string here to skip pairing
SESSION_ID=

# Optional: OpenWeather API key (the bot uses open-meteo by default, no key needed)
OPENWEATHER_API_KEY=

# Optional: OpenAI API key for .ai command
OPENAI_API_KEY=
